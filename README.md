# WormholeAftermath - facility utilities (branch: feature/mathlib)

This branch adds a small procedural math library (facility/mathlib.py) and the add-on scaffolding
so we can iterate on Blender integration without losing history.

Next steps:
- Review the files above.
- If OK, reply "Yes, commit these files" and I will push them to branch feature/mathlib.
- After commit I can:
  - Add the rest of the add-on operator code (full UI + generator calls),
  - Add hex/triangle layouts and generator changes,
  - Open a PR against the default branch if you'd like to merge later.

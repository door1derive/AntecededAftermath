[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "facility"
version = "0.0.0"
description = "Procedural facility / floor generation helpers"
requires-python = ">=3.8"
